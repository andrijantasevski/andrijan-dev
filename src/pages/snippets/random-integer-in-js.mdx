---
layout: "../../layouts/LayoutBlogSnippets.astro"
title: Random integer
author: Andrijan Tasevski
shortDescription: Generate random inclusive and non-inclusive integers
dateUpdated: 11 Oct, 2022
id: 1
---

## Generating a random non-inclusive integer

```javascript
function getRandomInt(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min) + min); // The maximum is exclusive and the minimum is inclusive
}
```

## Getting a random inclusive integer

```javascript
function getRandomIntInclusive(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min + 1) + min); // The maximum is inclusive and the minimum is inclusive
}
```
